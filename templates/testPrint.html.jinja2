<html>
    <head>
        <title>Debug page</title>
    </head>

    <body>
    <p>DATABASE DISPLAY :</p>
    <div>
    <p>ORGANISATIONS :</p>
        {% for orga in organisations %}
            <p> Organisation : {{orga.name}} (id={{orga.id}})</p>
            <p>liste des personnes dans cette entité : {{ orga.postes }}</p>
        {% endfor %}

    <br><br>
    </div>
    <div>
    <br>
        <p>POSITIONS :</p>
        {% for p in positions %}
            <p>  {{p.titre}}(id={{p.id}}), </p>

        {% endfor %}
    <br><br>
    </div>
    <div>
        <p>LIEN ENTRE POSITIONS/ORGANISATIONS</p>
        {% for orga in organisations %} {{ orga.name }} :
            {% for poste in orga.postes %}
                {{ poste.titre }},
            {% endfor %}<br>
        {% endfor %}

        <br><br>
    </div>
    <div>
        <p>PFEs : </p>
        {% for pfe in pfes %}
            <p>projet {{ pfe.titre }} : {{ pfe.description }}</p>
        {% endfor %}
    </div>
    <div>
        <p>TAFS : </p>
        {% for taf in tafs %}
             {{ taf.name }},
        {% endfor %}
    </div>
    <div>
        <p>PERSONNES : </p>
        {% for t in personnes %}
             {{ t.name }},
        {% endfor %}
    </div>


    <!--
     </p> TEST AFFICHAGE
            <form method="POST">
            <input name="testFiltrer">
            <button type="submit">Test fonction filtrer</button>
            <p>{% for a in ans %}</p>
            {{ a }}
            {% endfor %}
            </form>

    -->
    </body>
</html>