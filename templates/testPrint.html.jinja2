<html>
    <head>
        <title>Debug page</title>
    </head>

    <body>
    <p>DATABASE DISPLAY :</p>
    <div>
    <p>ORGANISATIONS :</p>
        {% for orga in organisations %}
            <p> Organisation : {{orga.name}} (id={{orga.id}})</p>
            <p>liste des personnes dans cette entité : {{ orga.postes }}</p><br>
        {% endfor %}

    </div>
    <div>
        <p>liste des positions existantes :</p>
        {% for p in positions %}
            <p>  {{p.titre}}(id={{p.id}}), </p>

        {% endfor %}
    </div>


    <!--
     </p> TEST AFFICHAGE
            <form method="POST">
            <input name="testFiltrer">
            <button type="submit">Test fonction filtrer</button>
            <p>{% for a in ans %}</p>
            {{ a }}
            {% endfor %}
            </form>

    -->
    </body>
</html>